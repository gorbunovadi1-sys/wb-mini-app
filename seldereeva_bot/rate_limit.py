"""WB-шный финансовый отчёт троттлится 1 запросом/мин НА АККАУНТ, а не на
инстанс WBClient (см. backend/wb_client.py:_finance_post) — если запустить
margin.build_margin_summary/dashboard_builder.build_dashboard_html из двух
команд бота одновременно (например /dashboard и следом /finance, или ручная
команда наложилась на понедельничный cron), у каждой свой WBClient со своим
_last_finance_call=0.0, и обе считают, что свободны звать API прямо сейчас —
реально они бьют в один и тот же лимит и обе начинают ловить 429.

Подтверждено живьём 2026-09-17: наложившиеся вызовы держали finance-api в
устойчивых 429 несколько минут, у одного из вызовов исчерпался запас
ретраев (5 попыток) и он упал с необработанным HTTPError — пользователю
это выглядело как «бот не среагировал», хотя на самом деле это была
~8-минутная пробуксовка, закончившаяся ошибкой.

Лок общий на процесс — любой вызов, который трогает WB finance-report API
(margin.fetch_rows) или advert-fullstats (тоже ловил 429 в том же
инциденте), должен держать его на время своей работы. threading.Lock, не
asyncio.Lock — тяжёлая работа идёт в asyncio.to_thread (реальный поток ОС),
поток блокируется на .acquire(), event loop продолжает обслуживать другие
апдейты."""
import threading

wb_finance_lock = threading.Lock()
